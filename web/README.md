```sh
pnpm create vite my-app --template react-ts

pnpm i -D tailwindcss postcss autoprefixer tailwindcss-animate
npx tailwindcss init -p
# update tailwind.config.js, globals.css

pnpm i react-router react-router-dom @emotion/react prettier eslint-config-prettier eslint-plugin-prettier
pnpm i -D @types/node

# change tsconfig.json
```
